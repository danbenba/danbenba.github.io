"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const commands_1 = __importDefault(require("./commands"));
const resources_1 = __importDefault(require("../resources"));
function validateCommand(argv, level) {
    let keys = Object.keys(commands_1.default.commands);
    if (level === 1) {
        keys = Object.keys(commands_1.default.commands[argv._[0]].commands || {});
    }
    if (argv._.length > level && !keys.includes(argv._[String(level)])) {
        throw new Error(resources_1.default.messages.cli.NO_SUCH_COMMAND(String(argv._[level])));
    }
    return true;
}
exports.default = validateCommand;
