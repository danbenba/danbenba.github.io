import { Logger } from '../../logger';
export default function initTemplate(options: {
    componentPath: string;
    templateType: string;
    componentName: string;
    compiler: string;
    logger: Logger;
}): Promise<{
    ok: true;
}>;
