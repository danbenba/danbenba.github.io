import { Component, TemplateInfo } from '../../types';
export default function preview(vm: {
    href: string;
    component: Component;
    qs: string;
    liveReload: string;
    templates: TemplateInfo[];
}): string;
