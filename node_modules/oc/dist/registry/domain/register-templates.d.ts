import { Template, TemplateInfo } from '../../types';
export default function registerTemplates(extraTemplates: Template[], dev?: boolean): {
    templatesHash: Record<string, Template>;
    templatesInfo: TemplateInfo[];
};
