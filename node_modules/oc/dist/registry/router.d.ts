import type { Express } from 'express';
import type { Repository } from '../registry/domain/repository';
import { Config } from '../types';
export declare function create(app: Express, conf: Config, repository: Repository): void;
